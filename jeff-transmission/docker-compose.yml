version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: jeff-transmission_launcher_1
      APP_PORT: 80

  launcher:
    image: nginx:alpine
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data/index.html:/usr/share/nginx/html/index.html:ro
